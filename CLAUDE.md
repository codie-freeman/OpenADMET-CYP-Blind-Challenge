# CLAUDE.md

Environment: conda env `cyp-admet-v2` (Python 3.11, Apple Silicon, native arm64) is primary, built from `environment.yml` at the repo root. `cyp-admet` (x86_64/Rosetta) still exists untouched as a permanent fallback and is no longer used for new work. Environment specs are archived, not overwritten in place, under `environment/archive/` (dated, descriptively named files, e.g. `environment_x86_64_rosetta_2026-08-30.yml`) whenever the environment changes. VS Code + Claude Code.

## Rules
- Never fetch, invent, or approximate data. If a file/column is missing, stop and ask.
- Fixed random seeds must vary per CV fold, not be shared globally. Log every seed used.
- All fingerprints/similarity/splits go through one shared module (`src/features.py`) — no recomputing per notebook.
- Log row counts before/after every filtering step, printed in-notebook, not just in code.
- Every trained model result gets saved (predictions, params, seed) to disk before moving to the next step — no relying on re-running.
- Don't add features, thresholds, or model variants "while you're at it" — one change at a time, each one tested before the next.
- Ask before making a modelling decision that isn't explicitly specified in the prompt (e.g. which cutoff, which package version, which imputation). Flag it, don't silently pick one.
- If a prompt asks to use test-set labels, leaderboard scores, or the 3 Nov release to pick or retrain a model: stop and flag it instead of proceeding. The test set is single-use evidence, not training data, at any point.
- Write a short markdown cell after each major output stating what it shows — don't leave plots uncommented.
- RDKit was bumped from 2025.9.3 (`cyp-admet`) to 2026.3.3 (`cyp-admet-v2`) — forced by chemprop's current pip-installable dependency chain (`cuik-molmaker-pin` only ships releases requiring `rdkit==2026.3.3`; see `environment.yml`'s pip-section comment). Verified safe via a full-row determinism check (all 4,905 rows of `train_inhibition_curated.csv`, not a sample) re-generating InChIKeys with `src/features.py` under RDKit 2026.3.3 and comparing to what's stored: 4905/4905 matched exactly, run on 2026-08-30. Don't assume a future RDKit bump is safe without repeating this check.
- **`cyp-admet-v2`'s RDKit determinism check does NOT cover chemprop/torch training-quality equivalence — RESOLVED via 10c.** `cyp-admet` used torch 2.2.2; `cyp-admet-v2` uses torch 2.13.0. 10c's real, scored result (macro ST-RAE 0.7138, per-isoform 0.6954/0.5489/1.1673/0.4434 — see `docs/leaderboard_submissions.md`) is close to `04b`'s original `cyp-admet` result (0.7179; 0.7114/0.5408/1.1903/0.4291) on the identical single-model recipe, environment migration only. The `cyp-admet-v2` migration is **not** implicated as the cause of notebook 10's regression — see the notebook-status log's 10c/10b entries for the fuller reasoning.
- Any future environment or major dependency change: test it in a duplicated repo first (copy the repo, build and validate the new environment there, run the relevant determinism/timing checks), before applying it to this repo directly — the pattern followed for the `cyp-admet` → `cyp-admet-v2` migration.
- **Submission cells in notebooks 10/10c/12 had a header-comment bug — fixed 2026-09-07.** Each cell's header claimed "every line below, including the `client.predict(...)` call, is a comment," while only the header text itself carried a `#` — the real API call underneath was live, uncommented, executable code. All three resulting submissions (nb10, 10c, nb12) were nonetheless **deliberate, reviewed, hand-run actions** by the user, exactly as this project's manual-gated-submission process requires — the content each one sent was correct and intended. The bug was purely in the cell's own self-description, never in the submission process, and it is easy to misread the file evidence alone as "fired by accident" (an earlier diagnostic pass over this repo did exactly that, incorrectly — corrected here). All three cells now comment out every line of the real API call, matching `04b_final_submission.ipynb`'s own cell 4 (the one template in this repo that was always genuinely, verifiably fully commented) — verified by extracting each fixed cell's code and confirming it executes as a true no-op (no output, no import, exit 0) before saving. See `docs/leaderboard_submissions.md` for the full scored record of all four real submissions.
- **CV-measured improvements are not adoption-ready on a bare p<0.05.** Selecting the best-scoring candidate out of several similar options, using the same CV data used to score them, is a known overfitting risk (Caruana et al. 2004) — see notebook 08/10/10b history below for a real, costly instance of this. Any future model/ensemble adoption decision needs an explicit, pre-stated effect-size or resolution threshold (how big a difference can this comparison actually resolve, given both row-sampling and training-seed noise — not just row-sampling), frozen before looking at results, same discipline as existing outlier/recalibration thresholds.
- **Ensemble candidate pools are evaluated on residual correlation (prediction − true), not raw prediction correlation.** Two models can have highly correlated raw outputs simply because both track the same true values, while still making usefully different errors (Dietterich, 2000). Diversity checks must decorrelate errors, not predictions.
- **The vendored evaluator's provenance vs. the live leaderboard backend has never been directly confirmed** — `src/vendor/openadmet_eval/` was pulled from `github.com/OpenADMET/CYP-Challenge-Tutorial` at some point and kept unmodified since; it's never been diffed against that repo's current HEAD to confirm it still matches what's actually live. Worth periodically checking, especially before treating a CV-vs-blind discrepancy as fully explained by modeling reasons alone.

## Notebook status log
Keep this current whenever a notebook is added or a prior notebook's result is superseded. A cold-start Claude Code session should be able to read this section alone and understand what's active vs. historical, without needing prior chat context.

- **00–02**: data curation, schema audit, chemical space exploration. ACTIVE, foundational.
- **03**: frozen 5×5 repeated CV fold assignments (`cv_folds.csv`, seed 42) + tabular feature sets. ACTIVE, frozen — never touched after this point.
- **03b**: log2fc-pretrained Chemprop encoder embeddings. Built, NOT YET ABLATED into any downstream model. Paused pending a leakage check (`scripts/train_log2fc_encoder_blind.py` exists, not run to completion).
- **04a**: single-fold (not full 5×5) screen of 21 configs. HISTORICAL — superseded by 05's properly-powered comparison for config selection purposes. Its winning config (`chemprop_chemeleoninit`) fed 04b's submission before 05 existed.
- **04b**: first live submission — single, unensembled `chemprop_chemeleoninit`, full-data retrain, config chosen by 04a. Result: macro ST-RAE 0.7179, rank 21/~23. ACTIVE as the reference baseline every later submission is compared against.
- **04c**: submission diagnostics for 04b. Ruled out systematic bias, CI width, NN similarity, activity-cliff proximity as CYP2D6-specific explanations. Leading hypothesis: blind test set construction (CYP2D6 not enrichment-seeded) — independently CONFIRMED via OpenADMET Discord (Sean Colby, 2026-09-03): CYP2D6 uses a different assay (Echo-MS vs. fluorescence) and shares its test compounds with the other three isoforms rather than being separately hit-expansion-constructed.
- **05**: full 5×5 CV (25 folds) comparison, ALL 11 real configs (9 tabular incl. XGBoost, 2 chemprop) + naive baselines, Ash et al. 2025 statistical protocol. ACTIVE, foundational — the authoritative CV comparison. Note: the "curated 6-config subset" figure sometimes referenced is a display subset for the main text, not the full comparison — always pull from `outputs/05_cv_comparison/`, not the subset plot.
- **05b**: Tanimoto/Butina-clustered (threshold 0.4, per 02's own validated threshold) alternative 5×5 CV split, `chemprop_chemeleoninit` only — tests whether a cluster-aware split (whole similarity clusters kept on one side of train/held-out per fold, unlike 05's/03's compound-independent RepeatedKFold) gives a CV estimate closer to `10c`'s real blind result. Entirely new files/outputs (`outputs/05b_cluster_cv_comparison/`, `scripts/05b_run_cluster_cv.py`); `03`'s `cv_folds.csv` and all of `05`'s saved outputs are untouched (confirmed via `git status` after the run — only an additive `src/features.py` function, `butina_clusters`, plus new files). **Flagged decision, confirmed with the user before building**: clusters are built independently per isoform as specified, but 26.7% of compounds carry labels for 2+ isoforms and `chemprop_chemeleoninit` is one shared multitask model per fold (single global split) — per-isoform clusters that share a compound are merged (union-find) into "coupled groups" before the random fold assignment, so no isoform's own cluster is ever split across train/held-out; verified both empirically before asking (largest merged group 0.73% of the data, no runaway supercluster) and formally after construction (0 violations checked directly against the final fold file). Same per-(repeat,fold) training seeds as `05` (seed 42 formula), reused verbatim and cross-checked against `05`'s manifest.csv at training-script startup, so fold assignment is the only variable under test. **Result: mixed, small-magnitude — not a clean win either way.** CYP1A2/CYP2C9 CV-to-blind gaps shrink slightly under the cluster split (0.1731→0.1716, 0.1298→0.1177); CYP2D6/CYP3A4 gaps widen slightly (0.1634→0.1660, 0.1027→0.1152). No significance test run between the two CV estimates (different fold structures, no common fold index — Ash's protocol doesn't apply). CYP2D6 does not stand alone here — it pairs with CYP3A4 (both favor the original split), complicating the "CYP2D6 is different because it wasn't hit-expansion-seeded" framing the notebook set out from. COMPLETE, inconclusive by design (one model, one dataset, single random draw per repeat) — nothing downstream depends on this result yet.
- **05c**: leakage-sensitivity check, not a blind-gap measurement — tests whether the non-`chemprop_chemeleoninit` configs that carried non-zero Caruana weight in NB12's CYP1A2/CYP3A4 ensembles (`chemeleon__rf`, `chemeleon__lightgbm`, `ecfp4_narrow__rf`, `ecfp4_narrow__lightgbm`, `mordred_pca__lightgbm`, `chemprop_randominit` — confirmed against `outputs/11b_caruana_selection/weights.csv` directly, not assumed) swing more between random-CV and 05b's cluster-CV than `chemprop_chemeleoninit` did, as a second, independent candidate mechanism for NB12's CYP1A2/CYP3A4 blind regression alongside 11b/12b's spread-compression finding. Entirely new files/outputs (`outputs/05c_cluster_cv_leakage_sensitivity/`, `scripts/05c_run_cluster_cv_randominit.py`); reuses 05b's `cluster_cv_folds.csv` read-only (no clustering/fold assignment rebuilt), 05's own `load_feature_matrix` code unchanged, and 05's per-(repeat,fold) seeds cross-checked against its manifest.csv. Tabular configs fit in-notebook (RF on 2048-dim embeddings turned out much slower than assumed, ~80s/fold not "fast" — a real timing surprise, not a design error — first nbconvert attempt hit a 1200s cell timeout partway through `chemeleon__rf`; resumed cleanly via the same on-disk-file `is_done` contract used throughout this project, no work lost); `chemprop_randominit` trained full 4-target multitask via a standalone background script (~40s/fold, ~17 min total), scored on CYP1A2/CYP3A4 only — training scope deliberately not narrowed, to avoid notebook 06's documented multitask-loss-reweighting risk. **Result: a real, isoform-specific asymmetry.** CYP1A2: all 6 configs' shifts are small (0.26–1.44%) and direction-mixed (4/6 leakage-consistent) — not a meaningful signal. **CYP3A4: all 6 configs shift in the leakage-consistent direction, at 2–4x `chemprop_chemeleoninit`'s own 2.29% reference shift** (2.31–8.19%) — a real, unanimous signal, on the same isoform where NB12's blind regression was proportionally worse (CYP3A4 +37.5% relative vs. CYP1A2 +9.8%, `10c`→NB12). Consistent with, not proof of, leakage-driven CV inflation contributing to CYP3A4's bad Caruana weighting specifically. COMPLETE — no significance test run (no shared fold index, same reasoning as 05b); cannot and does not measure blind performance for any of these 6 configs (none was ever submitted individually).
- **06**: CYP2D6 outlier exclusion (residual- and CI-width-based). Residual exclusion helped `chemeleon__rf`, `chemeleon__lightgbm`, `chemprop_chemeleoninit` in CV. Sections 5–8: found excluding CYP2D6 labels shifts `chemprop_chemeleoninit`'s predictions on OTHER isoforms too (untouched labels) — leading unconfirmed hypothesis is Chemprop's multitask loss reweighting by valid-label count. COMPLETE, UNRESOLVED caveat carried forward — `chemprop_chemeleoninit` deferred from further CYP2D6-specific tuning in 07 because of this.
- **07**: CYP2D6 sample weighting + hyperparameter tuning, tabular configs only. Weighting: negative for both configs. Tuning: helped `chemeleon__rf` (adopted, tuned params), not significant for `chemeleon__lightgbm` (06 baseline kept). COMPLETE.
- **08**: per-isoform ensemble selection via 05's CLD tiers, simple averaging, subset search over each isoform's small tied pool, kept whichever combo scored best in CV. Adopted ensembles for CYP1A2 (4-way), CYP2D6 (2-way), CYP3A4 (2-way); CYP2C9 no ensemble (singleton winner). COMPLETE but **SUPERSEDED as a selection method** — see 10/10b. Its per-isoform CV results are still valid data, reused as input to notebook 11's wider candidate pool.
- **09**: placement/recalibration diagnostic (R² = 2ρk − k² − b² decomposition, following SuperCowPowers/workbench's public write-up, cited: `supercowpowers.github.io/workbench/blogs/cyp_challenge/`). Found k+b share of the R² gap under 10% (frozen threshold) for all four isoforms — no correction adopted, all four isoforms use identity recalibration (slope=1.0, intercept=0.0). COMPLETE, negative result, real finding (ranking quality dominates the gap, not placement).
- **10**: full retrain + predict + submit using 08's ensemble winners + 09's (identity) recalibration. Second live submission. Result: macro ST-RAE 0.8299 — WORSE than 04b on every isoform, including CYP1A2/CYP3A4 which 06's exclusion never touched. COMPLETE, regression confirmed, root cause NOT fully resolved.
- **10b**: investigation into 10's regression. Spread-compression hypothesis tested — weakened under the correct metric (ST-RAE): direction inconsistent between CYP1A2 and CYP3A4. Selection-bias mechanism (Caruana et al. 2004) identified as a plausible contributor for CYP1A2/CYP3A4 specifically (NOT CYP2D6 — CYP2D6 was already broken pre-ensembling, R²=-0.62 in 04b, unrelated cause). CV margins that justified 08's ensembles were fully erased on blind data for all three ensembled isoforms. COMPLETE, root cause still not definitively isolated — see 10c.
- **10c**: control submission — plain, unensembled `chemprop_chemeleoninit`, full retrain under `cyp-admet-v2`, no CYP2D6 exclusion. Purpose: isolate environment migration vs. 08's ensembling/exclusion as the cause of 10's regression. **SUBMITTED 2026-09-04 ~10:15 local** (outside any Claude Code session — done directly in the notebook, deliberately, after review; confirmed via the notebook's own saved output: "Submission received from 'fold-zero'... should appear on the leaderboard within 2 hours"). **SCORED**: macro ST-RAE 0.7138 (per-isoform 0.6954/0.5489/1.1673/0.4434) — close to `04b`'s 0.7179, resolving the Rules-section environment-migration question: **the `cyp-admet-v2` migration is not the cause of notebook 10's regression.** Full metric breakdown in `docs/leaderboard_submissions.md`. Incidentally resolves an open question 10c's own intro flagged: "one submission per lab" is a **cooldown, not a lifetime cap** — this submission succeeded after notebook 10's earlier one, so the allowance does refresh, and notebook 12's later submission (below) confirms it refreshes more than once.
- **11**: Caruana ensemble-selection prep — assembles one pooled (repeat, fold, inchikey)-level OOF table per isoform across all 25 CV folds, all 11 real configs from 05 (not just 08's CLD-tier-a subset), plus matched true/conf_low/conf_high from the curated data. Did not exist before 2026-09-03 despite this log's own prior entry (sitting in an uncommitted diff) describing it as already built — that claim did not match the repo's actual state (confirmed via full filesystem/git-history search) and was flagged to the user before building it for real, from already-computed 05 predictions, no retraining. COMPLETE.
- **11b**: Caruana bagged ensemble selection (Caruana et al. 2004 Sec. 2.1–2.3: sorted top-1 init, greedy selection-with-replacement with early stopping, bagged over random 50%-of-library subsets — `src/ensemble/caruana.py`; hyperparameters `n_bags=20, bag_frac=0.5, n_init=1, max_iter=50, seed=42`, matching `jeremycheminf/openadmet_scripts`'s `CYP_Challenge/src/cyp_submission/caruana.py`) run per isoform against 05's full 11-config pool, scored on real ST-RAE. **REVISED IN PLACE 2026-09-04, WHY: 12b's spread-compression finding (below) — original (uncapped) result superseded.** Original per-isoform verdicts vs. 08/single-best (still true of the uncapped selection, preserved in `weights_uncapped.csv`): beat 08 for CYP1A2/CYP2C9, no different for CYP3A4, worse for CYP2D6; beat single-best-of-11 everywhere. **Revision adds two new, user-gated steps, both frozen BEFORE results per this project's threshold discipline:** (1) a marginal-CV-value trajectory per isoform (`outputs/11b_caruana_selection/marginal_value_trajectory.csv` + required plot) revealed CYP2C9/CYP3A4 have a genuine interior CV-ST-RAE minimum, not just a plateau — the full uncapped ensemble is measurably *worse* in CV than a smaller subset (CYP2C9: 2.17% relatively worse). User-confirmed cap rule: empirical CV-minimizing `k` per isoform (not a % threshold) → CYP1A2 10→6, CYP2C9 9→3, CYP2D6 11→8, CYP3A4 9→5 members; capping confirmed (not assumed) to both improve CV ST-RAE and reduce CV-side spread compression for all 4 isoforms (`cap_comparison.csv`). (2) A fresh placement-correction diagnostic (09's ρ/k/b formula, NOT 09's own params — computed on the capped ensemble's own pooled OOF) found k+b share of the R² gap 0.66–2.15% across isoforms; user-confirmed reuse of 09's 10% pursuit threshold → no correction pursued for any isoform, identity recalibration saved to this notebook's **own** `recalibration_params.csv` (distinct from 09's file). `weights.csv` now holds the capped, renormalized result (canonical); `weights_uncapped.csv` preserves the original. COMPLETE. Still CV-only, still not itself a submission decision. **CYP2D6 pool-composition note (documented explicitly 2026-09-07, in a new markdown cell in this notebook):** every isoform's candidate pool, including CYP2D6's, was built from 05's plain, unexcluded OOF predictions only — never 06's residual-excluded `chemprop_chemeleoninit` or 07's tuned `chemeleon__rf`, the two configs notebook 08's own CYP2D6 ensemble actually used. This was a **deliberate isolation test**, not an oversight: it asks whether CYP2D6's persistent poor ensembling performance comes from ensembling/selection mechanics themselves or specifically from 06's residual-exclusion approach to its training data. Once notebook 12's blind result came back, CYP2D6 was still the single worst-performing isoform under ensembling (+0.242 ST-RAE vs. 10c, the largest degradation of any isoform) despite zero exposure to any of 06's excluded-data variants — **this rules out 06's residual exclusion specifically as the cause.** It does **not** rule out whether a CYP2D6-specific pool that does include 06/07's variants would do better — that stays open, reserved as a later, separate rebuild. **Correlation-matrix diagnostic added 2026-09-13 (new section, end of notebook, no retraining):** checks whether CYP1A2/CYP3A4's Caruana-selected members (the two isoforms whose ensembles regressed on real blind performance) are genuinely decorrelated or mostly redundant. Computed on **residual (prediction − true) correlation**, not raw OOF-prediction correlation — a deliberate deviation from the section's own literal request, flagged in-notebook, because raw-prediction correlation is the wrong diagnostic per this file's own rule below (Dietterich 2000); raw-prediction correlation is still shown alongside for reference. Both checks came back **CONFIRMED** for both isoforms: `chemeleon__*` tabular configs correlate more with `ecfp4_narrow__*`/`mordred_pca__*` than with chemprop (redundant-descriptor concern reproduced in this project's own pool), and chemprop (graph-based) is more decorrelated from the tabular pool than the tabular families are from each other (real diversity axis is graph-vs-tabular, not feature-representation choice within tabular) — though all absolute residual correlations are high in this pool (~0.79–0.96), so this is a difference of degree, not a clean split. Selected-members-only mean residual correlation (CYP1A2 0.880, CYP3A4 0.781) is close to, barely below, the full-pool mean (0.884, 0.792) — the selected subset is not meaningfully more decorrelated than the rejected candidates were. New outputs only (`correlation_matrix_{CYP1A2,CYP3A4}.csv` + raw-prediction/family-summary/selected-vs-pool CSVs and figures in `outputs/11b_caruana_selection/`); `weights.csv`/`recalibration_params.csv`/every other existing 11b output untouched. Descriptive only — consistent with, does not prove, redundancy contributing to the two isoforms' spread-compression-driven blind regression; no adoption/submission decision made.
- **12**: Caruana-weighted full retrain + blind predict. **REVISED IN PLACE 2026-09-04, WHY: rebuilt on 11b's revised (capped) weights, per the spread-compression finding — no new training needed**, since capping only subsets configs `scripts/12_caruana_retrain_predict.py` already trained/reused in its one and only run (8 of 39 original pairs reused from notebook 10, incl. a real first-ever full-data `chemprop_randominit` run, 57.6s wall time — see prior entry text, unchanged by this revision). Recalibration now reads `outputs/11b_caruana_selection/recalibration_params.csv` (11b's own, capped-ensemble-specific check), not 09's file. Previous (uncapped) submission preserved as `outputs/12_caruana_retrain_predict/submission_candidate_uncapped.csv` before being overwritten by the capped rebuild, specifically so 12b's revision could confirm the cap's actual effect rather than assume it. Re-validated PASS via `validate_activity_submission`. **SUBMITTED 2026-09-04 21:28 UTC** (deliberately uncommented and run by hand after review, per this project's manual-gated-submission process — confirmed via the notebook's own saved output and the submission file's sha256/mtime). **This entry previously stated "Still NOT submitted — gradio_client cell re-verified fully commented after the rebuild"; that was wrong** — the cell had the same header-comment bug as notebook 10's and 10c's (see the Rules-section note), now fixed. The submission itself was intended and its content correct. **SCORED**: macro ST-RAE 0.8299 (per-isoform 0.7633/0.5375/1.4092/0.6095) — full metric breakdown and the CYP2C9 monotonic-improvement finding in `docs/leaderboard_submissions.md`. Same CYP2D6 pool-composition scope as before (unchanged by this revision — see 11b's entry above for why this is deliberate and what it does/doesn't rule out).
- **12b**: pre-submission diagnostic, notebook 12 vs. 10c. **REVISED IN PLACE 2026-09-04, WHY: reruns the comparison against 12's revised (capped) submission, and adds a before/after-capping check using the preserved uncapped submission.** Real, unsoftened finding, both halves: **capping genuinely widened blind-set prediction spread for all 4 isoforms** (confirmed on real blind predictions, not just CV — e.g. CYP1A2 0.546→0.556, CYP2C9 0.588→0.639, CYP2D6 0.358→0.365, CYP3A4 0.828→0.849) — the cap's intended effect is real, not just theoretical. **But it did not fully close the gap**: notebook 12 (capped)'s spread is STILL narrower than 10c's for all 4 isoforms (e.g. CYP1A2 0.556 vs 10c's 0.862 vs training-true 1.031) — capping is a genuine, measured partial mitigation, not a fix, and this notebook reports that plainly rather than rounding up "wider than before" to "fixed." Agreement with 10c (capped): r=0.84–0.96, mean|diff| 0.23–0.38 pIC50 units, tracking each isoform's non-`chemprop_chemeleoninit` weight share (61–90%, all somewhat lower than the uncapped version's 74–91% since capping trims some non-chemprop members). Top-5 disagreement compounds and `summary.csv` both overwritten to reflect the capped comparison. COMPLETE, descriptive only, still cannot and does not forecast leaderboard outcome — 10c remained submitted but not yet scored as of this revision. **Update 2026-09-07:** both 10c and notebook 12 have since been submitted and scored (macro ST-RAE 0.7138 and 0.8299 respectively) — see `docs/leaderboard_submissions.md` for the full comparison. This notebook's own capped-vs-uncapped spread analysis (CV-side and blind-prediction-side) is unaffected by that update; it was never contingent on the real scores coming in.
- **13 (historical, number since reused — see the new "13" entry below)**: placement-correction retest, aimed at spread (a diagnostic-report next-step option, "5A"). Reran the nested-CV-validated OLS correction `09`/`11b` both derived but never executed, properly out-of-fold this time, on the capped Caruana ensemble's already-computed pooled OOF predictions — no retraining. Result: **not adopted for any isoform** — CYP1A2/CYP2C9/CYP2D6 significantly *worse* under the correction (not just insufficiently better), CYP3A4 flat/non-significant. A real, clean negative result: the correction that would widen prediction spread also makes the scored metric worse when tested honestly out-of-fold. **REMOVED 2026-09-10** — user decision, abandoned as a dead end rather than kept as an active or historical reference. The notebook (never committed to git) has been deleted; its saved output (`outputs/13_placement_correction_retest/{nested_cv_results,recalibration_params,blind_spread_diagnostic}.csv`) is left on disk but is no longer reproducible from a live notebook. If this correction is ever revisited, note it here first — it has already been tried once, out-of-fold, and failed cleanly.
- **14**: CYP2D6-revert submission candidate (a diagnostic-report next-step option, "5B"). Built a pure recombination (no training): CYP2D6 ← `10c`'s single-model prediction, CYP1A2/CYP2C9/CYP3A4 ← notebook 12's ensemble prediction, per the "revert CYP2D6 only" rule the user confirmed at the time. Validated PASS via `validate_activity_submission`. Never submitted from within this repo (submission cell's `execution_count` stayed `None`). **REMOVED 2026-09-10** — user decision: this candidate was not adopted or submitted from this repo and is no longer being pursued. The notebook (never committed to git) has been deleted; its saved build output (`outputs/14_cyp2d6_revert_submission_candidate/`) is left on disk but is no longer reproducible from a live notebook.
- **13** (number reused 2026-09-12 — unrelated to the removed placement-correction-retest notebook logged above under the same number; no project rule retires deleted notebook numbers): AID 1851 (PubChem, NCATS qHTS cytochrome panel — `scripts/13_fetch_aid1851.py`, 17,143 SIDs / 16,560 unique CIDs, batched under the 10,000-SID PUG REST cap) fetch, curation, and a new external-population calibration module (`src/calibration.py`), per two real entrants on this exact live challenge (SuperCowPowers, jeremycheminf) reporting a large improvement from recalibrating OOF predictions onto an *external* reference population's mean/std rather than this project's own training-population moments (which `09`/`11b` already tried and found not worth pursuing, kb_share <10% for all four isoforms). AID 1851 used strictly as a population-moment estimate — never as training/fine-tuning data. Real structure inspected rather than assumed: **long format**, not per-isoform columns — one row per (compound, isoform-panel) via a `Panel Name` field; confirmed via three reference inhibitors (furafylline/CYP1A2, sulfaphenazole/CYP2C9, ketoconazole/CYP3A4 all showed correctly isoform-selective, strongly negative `Fit_LogAC50`; quinidine wasn't tested as a library SID in this assay). Sign convention confirmed empirically (not assumed): `Fit_LogAC50` = log10(AC50 in M), so `pIC50_equivalent = -Fit_LogAC50`. Inactive/no-fit compounds (49.7% of rows) floored at `-log10(row-specific top tested concentration in M)` (user-confirmed approach) — the top concentration genuinely varies per row (57.14/25.56/11.43/2.286/0.457 µM), not one constant, so the floor is computed per row. Overlap with this project's own train/blind compounds: tiny (0.13–0.30% per isoform, none past the 5% flag threshold) — excluded before computing moments. **A real, unsoftened finding that contradicts the task's own motivating premise, reported plainly rather than smoothed over**: the two cited entrants reportedly found their *largest* population-mean gap at CYP2D6; this AID-1851-based estimate does not reproduce that — CYP2D6's own-vs-AID1851 gap is small and in the *opposite* direction (-0.10), while **CYP3A4 shows by far the largest gap (+0.83)**, most likely because this project's own CYP3A4 training-label mean (4.10) already sits unusually low relative to the other three isoforms (4.58–4.96), a pre-existing project-specific fact rather than a CYP2D6-specific effect. Per-isoform moments in `outputs/population_moments/aid1851_moments.csv`. Calibration (`fit_blind_population_calibration`/`apply_calibration`, a rho-shrunk variance-matching transform, distinct from `09`/`11b`'s own-population OLS placement correction) applied to each isoform's actually-planned model — CYP1A2/CYP2D6/CYP3A4: `chemprop_chemeleoninit` OOF (via `outputs/11_caruana_prep/`, sourced from `outputs/05_cv_comparison/`); CYP2C9: notebook 12's capped Caruana ensemble OOF (`outputs/11b_caruana_selection/`) — params in `outputs/calibration_blind_population/params.csv`. COMPLETE — reporting only; no adoption/submission decision made here, and this cannot be validated against real blind performance (no blind labels exist until 2026-11-03). See **14** for a follow-up censored-MLE re-estimate of these same moments and the actual OOF ST-RAE test of whether calibrating against either version helps. **Submission-assembly cell added 2026-09-13** (Part 6, appended to the end of this same notebook — not a new notebook): builds a real blind-submission candidate applying this notebook's own floored-mean calibration (not 14's censored-MLE version, which 14 tested and rejected) **uniformly to all four isoforms**, deliberately including CYP2C9/CYP3A4 where 14's OOF test found this correction hurts — a intentional design choice to get a clean, isolated real-blind-data read on whether the calibration mechanism helps at all, not to assemble the best-guess recipe. Raw full-data blind predictions reused from disk, no retraining: CYP1A2/CYP2D6/CYP3A4 from `outputs/12_caruana_retrain_predict/blind_predictions/chemprop_chemeleoninit.csv` (confirmed bit-identical, max abs diff `0.0`, to `10c`'s actually-submitted, scored values); CYP2C9 from the capped Caruana ensemble (`chemprop_chemeleoninit` 0.388 + `chemprop_randominit` 0.224 + `ecfp4_narrow__rf` 0.388, per `outputs/11b_caruana_selection/weights.csv`), confirmed bit-identical (floating-point only, ~1e-15) to notebook 12's actually-submitted, scored values — explicitly confirmed to be the ensemble, not `chemprop_chemeleoninit` alone. `validate_activity_submission` returned **PASS** (750 rows, all required columns). Written to `outputs/13_aid1851_blind_population_calibration/submission_candidate.csv`. A `gradio_client` submission cell matching `04b`/`10c`/`12`'s own call pattern was prepared at the very end, every line genuinely `#`-commented (verified, matching the fixed convention, not the header-only bug), awaiting a deliberate, manual, human-run decision, per this project's established submission process. **That decision was made: SUBMITTED 2026-09-13 15:07 UTC** (uncommented and run by hand after review, alias `fold-zero`). **SCORED**: macro ST-RAE 0.8811 (per-isoform CYP1A2 0.8197 / CYP2C9 0.6036 / CYP2D6 1.3570 / CYP3A4 0.7443) — full metric breakdown, and the plain result statement, in `docs/leaderboard_submissions.md` (labeled `NB13-calib`). **Worse than both 04b and 10c on every single isoform**, including CYP1A2 and CYP2D6 — the two isoforms where this notebook's own OOF test (Part 3, echoed in `14`'s acid test) had predicted floored-mean calibration would help. Combined with `14`'s clean OOF-side negative result for censored-MLE and `09`/`11b`'s own-population negative result, **population calibration is now a confirmed real-blind-data negative result in every form tested (09, 11b, 13, 14), not just an OOF one** — none of it is adopted or recommended going forward.
- **14**: censored (left-censored, Tobit-style) maximum-likelihood re-estimate of AID 1851's per-isoform population moments, replacing `13`'s floored-mean point-substitution approach (which structurally cannot place a population mean below the lowest censoring floor actually observed, ~4.24 pIC50-equivalent) — motivated by SuperCowPowers specifically describing deriving CYP2D6's population center from the panel's *inactivity rate*, a method that, unlike a floored mean, can place a center below the assay's detection floor. The fit-vs-censored distinction was reconstructed fresh from the raw per-SID data (`data/raw/aid1851/batch_*.csv`) rather than reused from `13`'s `aid1851_curated.csv`, whose `any_censored` column is only an aggregate-level flag, ambiguous for the ~2.8% of compounds with multiple replicate SID submissions; replicates were reconciled per compound (exact if any replicate has a real fit, else censored at the *tightest* — minimum — replicate floor), and the reconciled population was confirmed identical to `13`'s own curated row counts (16,558/isoform) before fitting anything. Normal(mu, sigma) censored MLE via `scipy.optimize.minimize` (numerically stable `norm.logpdf`/`logcdf`, not `log(pdf)`/`log(cdf)`); convergence AND a 5-start-point global-optimum check both passed for all four isoforms (all five starts agree to <1e-4 in both parameters, every isoform). **Result: CYP2D6 moves from 4.686 (floored) to 3.802 (censored-MLE) — the specific gap this notebook exists to close, now close to SuperCowPowers' reported ~3.7**; CYP1A2/CYP2C9/CYP3A4 shift by smaller-but-real amounts (-0.32/-0.47/-0.30). Saved to `outputs/population_moments/aid1851_moments_censored_mle.csv` (`13`'s own `aid1851_moments.csv` untouched); calibration refit to `outputs/calibration_blind_population/params_censored_mle.csv` (`13`'s own `params.csv` untouched). **The actual point of this notebook — Part 3's acid test, does either calibrated version beat raw OOF ST-RAE — is a clean negative result for censored-MLE, stated plainly per isoform, none rounded up:** CYP1A2 and CYP2D6 favor floored-mean calibration (0.821 and 0.932 vs. raw's 0.868 and 1.004); CYP2C9 and CYP3A4 favor raw outright (0.617 and 0.546, beating both calibrated versions). **On CYP2D6 specifically — the isoform this notebook's better population estimate targeted — censored-MLE calibration is not just unhelpful but catastrophic (ST-RAE 2.259, over double raw)**, because the wider censored-MLE target std combined with CYP2D6's weak raw rho (0.369) drives an overcorrecting slope/intercept. **On CYP3A4 — flagged in advance as this project's best real-leaderboard performer and a risk to over-correct — both calibrated versions score worse than raw**, floored-mean the worse of the two (1.007 vs. 0.546). Censored-MLE calibration is not the best condition for any of the four isoforms despite being the more statistically principled population estimate. COMPLETE — OOF-only (no blind labels used or exist until 2026-11-03), no adoption/submission decision made.
- **15**: analog-holdout split — a single (not repeated-CV) train/test partition approximating how the REAL blind test set was actually constructed, per SuperCowPowers' own public description ("the top 25 hits for CYP1A2, CYP2C9 and CYP3A4, then the top 10 catalog chemisimilars purchased for each, giving 750 compounds"; CYP2D6 confirmed excluded from hit-expansion). Per isoform (CYP1A2/CYP2C9/CYP3A4, independently): top-25-by-pIC50 seeds always stay in training; up to 10 ECFP4/Tanimoto nearest neighbours per seed (threshold 0.4 — confirmed with the user beforehand, this project's own established "similar compounds" cutoff, matching notebook 02's SALI secondary threshold/04c/05b's Butina cutoff, via `src/features.py`'s `pairwise_tanimoto_matrix`, reused not reimplemented) are held out instead; a candidate claimed by two seeds goes to whichever it's more similar to. CYP2D6 gets a plain random holdout instead, sized to the other three isoforms' mean held-out proportion (~1.9%), per its confirmed exclusion from the real hit-expansion mechanism. Saved to `data/folds/analog_holdout_split.csv` (new file, alongside — not replacing — `cv_folds.csv`/05b's `cluster_cv_folds.csv`). **Key structural finding: the confirmed purchased-vs-existing-compound limitation bites hard** — at threshold 0.4, most top-25 seeds have ZERO real neighbours already sitting in the curated dataset (14/25 CYP1A2, 7/25 CYP2C9, 6/25 CYP3A4), so the resulting held-out set is only 129 of 4,905 compounds (101 analog neighbours + 28 CYP2D6 random), far smaller than a CV fold (~980) or the real blind set (750). `chemprop_chemeleoninit` retrained once on this split's training compounds via `scripts/15_run_analog_holdout.py` (single run, `SEED=42` throughout, matching this project's one-off-final-run precedent — not one of 05's per-fold manifest seeds), scored with the same vendored bootstrap evaluator as every other notebook. **Result: a clean, decisive negative — the opposite of the hoped-for outcome.** This split's gap-to-10c-blind (0.37–1.07 per isoform, 0.56 macro) is 2.3–9.3x LARGER than both `05`'s random-CV gap and `05b`'s cluster-CV gap (both 0.06–0.17) for every isoform, not smaller — confirmed outside this run's own bootstrap noise band (2-std) for CYP1A2/CYP2C9/CYP3A4/macro; only CYP2D6's own gap sits within its noise band, unsurprising given it's the smallest, plain-random-drawn portion (28 compounds). A real, checked mechanism (not just noise) behind the inflated gap: the model systematically OVERpredicts on this held-out set for all 4 isoforms (mean true-minus-predicted residual -0.12 to -0.47 pIC50 units, confirmed directly against real predictions) — plausibly because the held-out compounds are deliberately concentrated on potency extremes and seed-adjacency (near-neighbours of the 25 most potent known hits), and the model, having seen each seed's own extreme value, overshoots the neighbour's actual (elevated but less extreme) potency, an activity-cliff-like effect neither `03`'s random split nor `05b`'s symmetric cluster split reproduces. **This is a third negative result (after `05b`) on "is the CV-to-blind mismatch explainable by split methodology"** — per the task's own pre-stated framing for this outcome, this split's ongoing use in this project is limited to the error-driven investigation lens only (e.g. studying why seed-adjacent, high-potency compounds are hard), not model-selection work; `05`'s and `05b`'s own conclusions, and all of `05`–`12`'s model-selection work on the original random split, are unaffected. COMPLETE — comparison/report only, no adoption/submission decision made, consistent with every other diagnostic notebook in this project.
- **16**: solves for each isoform's true blind-population mean/spread using ONLY this project's own published board metrics (R², MAE, Spearman) across the four already-real submissions (`04b`/`10c`/NB10/NB12 — deliberately excluding today's NB13-calib submission as a solve input, since its own predictions were already transformed once) — a different calibration target from `13`/`14`'s external-AID-1851 approach, no external dataset used anywhere. Decomposition: `R² = 2·ρ·k − k² − b²` (`k=sd(pred)/sd(true_pop)`, `b=(mean(pred)-mean(true_pop))/sd(true_pop)`), published Spearman used as an explicit, stated proxy for Pearson `ρ` (not published on this leaderboard), MAE supplying a second equation under an explicit, stated Normality assumption on the `(true−pred)` residual (closed-form folded-normal `E|X|`) — both approximations stated prominently, result never presented as exact. **Real, unanticipated finding, discovered while building the solve, not assumed going in: the R²+MAE system is provably symmetric under `mean_true_pop → 2·mean(pred) − mean_true_pop`** (both R² and MAE are even functions of the bias term under the Normality assumption) — the *sign* of the population offset cannot be recovered from R²/MAE/ρ alone, for any submission, as a matter of the math, not a numerical artifact (confirmed via a 1-D reformulation of the solve that isolates `k` from the sign-dependent branch). **Two judgment calls flagged and confirmed with the user before proceeding** (both a real, not-fully-specified-in-the-prompt fork): (1) where the two mirror candidates aren't clearly separated by plausibility against this isoform's own training-label mean (ratio of distances <1.5×, an explicit, stated threshold), **the sign is reported as undetermined rather than forced** — consistent with this project's pattern of clean negative/inconclusive results (05b/05c/15) over a guessed number; (2) the direct consequence — CYP1A2 ends up with 0 well-determined solves (all 4 submissions sign-ambiguous) and CYP3A4 with only 1 (`04b`/`10c` algebraically INFEASIBLE — Spearman-proxy² < published R², impossible for a real Pearson-based R², meaning the Spearman-as-Pearson substitution genuinely breaks down for this isoform specifically; NB10 sign-ambiguous; only NB12 usable), both below the 2-solve minimum — so Part 4's correction to `10c` is applied **only to CYP2C9/CYP2D6**, leaving CYP1A2/CYP3A4 as `10c`'s unmodified raw predictions, a deliberate, flagged deviation from a literal "uniform across all four" application. **Result, per isoform**: CYP2C9 — clean convergence, all 4 submissions agree tightly (`mean_true_pop` 4.71–4.88, `sd_true_pop` 0.979–0.992), real evidence of a genuine submission-independent property; CYP2D6 — partial, only `04b`/`10c` survive the ambiguity check but agree closely (5.822 vs. 5.838; 1.513 vs. 1.515), final estimate produced from those 2; CYP1A2/CYP3A4 — no defensible estimate, reported plainly as undetermined rather than forced. **Held-out check (Part 3)** against today's NB13-calib submission (not used in the solve) — checkable only for CYP2C9/CYP2D6 (no Part 2 estimate exists for the other two) — found the forward-predicted R²/MAE directionally consistent but not quantitatively tight (CYP2C9: predicted R²=0.287 vs. actual 0.424, actual beat the prediction; CYP2D6: predicted R²=−0.585 vs. actual −0.790, prediction was too optimistic — the two isoforms don't even miss in the same direction), reported honestly as partial, inconclusive support rather than a confirmation. `outputs/population_moments/board_solved_moments.csv` (full per-submission traceability) and `outputs/board_solved_calibration/submission_candidate.csv` (validated PASS via unmodified `validate_activity_submission`) written; a `gradio_client` submission cell is prepared, fully commented out per this project's convention, and **not executed** — no adoption/submission decision made, left to the user. COMPLETE, no retraining anywhere, no existing output file touched. **CYP2D6 root-selection override added as an end-of-notebook addendum, same day.** `04b`/`10c`'s CYP2D6 solves had auto-resolved `OK` (not `AMBIGUOUS`) only because their ambiguity ratio (1.510, 1.531) sat just above this notebook's own 1.5× cutoff — a marginal call, resolved by the same closer-to-training-mean heuristic this project's true sign-tiebreak decision (above) already rejected as unreliable in general. A real external precedent specific to this isoform — a comparable real entrant on this exact challenge reporting their own solved CYP2D6 blind-population mean at pIC50 ≈3.107, below their raw predictions — points instead to the **lower** mirror candidate (~3.19) `04b`/`10c` had originally set aside. The addendum overrides to the lower root for `04b`/`10c` and checks whether the same convention resolves `NB10`/`NB12`'s already-excluded ambiguity too: **it does** — all 4 submissions' lower-root candidates cluster to a range of 0.091 (tighter than CYP2C9's own already-accepted 4/4 "tight clustering" range of 0.174), landing at mean≈3.156, within 0.05 of the external precedent (NB12 alone: 3.104, within 0.003) — a materially stronger, corroborated 4-of-4 result superseding the original 2-of-4 estimate (mean≈5.830). CYP2D6's correction was recomputed against this new target (same functional form, same 10c OOF rho, reused not recomputed) and now shifts 10c's CYP2D6 predictions **down** (raw 4.509 → ~3.16) instead of up; `outputs/board_solved_calibration/submission_candidate.csv` was overwritten accordingly (CYP1A2/CYP3A4 raw and CYP2C9's correction confirmed bit-identical to the original build, only CYP2D6 differs) and re-validated PASS. A new traceability file, `outputs/population_moments/board_solved_moments_cyp2d6_override.csv`, records this — the original `board_solved_moments.csv` is untouched. The notebook states plainly this override is a genuine test, not a settled finding — its correctness should be judged by the real board score, not assumed from the precedent alone. The (single, updated-in-place) `gradio_client` cell remains fully commented out and unexecuted. **Final check added as a second, small end-of-notebook section, same day**: reran Part 3's CYP2D6 forward-check (untouched original cell) against the override target instead of the old one, reusing `NB13-calib`'s already-computed prediction mean/std and OOF rho rather than recomputing them. Result, stated plainly: the override target predicts `NB13-calib`'s real, already-published CYP2D6 board score **substantially closer** than the old target did — R² off by 0.028 vs. the old target's 0.205 (~7× closer), MAE off by 0.037 vs. 0.132 (~4× closer). This is a second, independent piece of evidence for the override (it uses `NB13-calib`'s real board score, which Part A's NB10/NB12 convergence check never touched), alongside Part A's own convergence finding and the external precedent — still not treated as proof (the same Spearman-proxy/Normality approximations this whole notebook is built on still apply, and "closer" is not "exact"). CYP2C9's Part 3 check, and everything in Parts 1-4/addendum A-C, is untouched. No adoption/submission decision made; the submission file itself was not rebuilt or re-validated again (read-only analysis only).
- **17**: cheap diagnostic only — no training, no submission, no architecture work. Computes physiological-pH (7.4) net formal charge via Dimorphite-DL (dominant microstate, `precision=0.0` collapses enumeration to exactly one state per ionisable site) and Crippen logP (a stated proxy for FAF-Drugs4's logD(pH7), never presented as logD) for every compound in both the curated training set and the blind test set, then checks per-isoform whether charge carries signal against pIC50, to gate whether a later descriptor-augmented Chemprop run is worth building. Motivated by Kiani & Jabeen (2019)'s CYP2D6 decision tree branching on total charge, treated as weak evidence (their own reported AUCs are middling/class-imbalance-driven) rather than fact — this notebook tests the electrostatic mechanism (protonated basic N/Asp301 for CYP2D6; cationic Arg108/acidic ligands for CYP2C9) against this project's own data. **A real environment conflict was hit and resolved before any computation**: `pip install dimorphite-dl` silently downgraded the pinned `rdkit==2026.3.3` to 2025.9.6 (dimorphite-dl hard-requires `rdkit<2026`), breaking chemprop's `cuik-molmaker-pin` dependency; fixed immediately via `pip install --no-deps --force-reinstall rdkit==2026.3.3`, verified after the fact (dimorphite-dl still imports/runs correctly against 2026.3.3 despite its own pin; chemprop still featurizes a real molecule end-to-end via `cuik_molmaker`). Documented in a new `environment.yml` comment block (this pin cannot be expressed as a normal one-line pip entry — a fresh single-pass `pip install -r` of both packages together is a proven `ResolutionImpossible`, confirmed via `--dry-run`) and archived pre-change at `environment/archive/environment_cyp-admet-v2_2026-09-14_pre-dimorphite-dl.yml`. **Part 1 verification: PASS, 6/6** — four literature CYP2D6-relevant basic amines (propranolol, quinidine, desipramine, metoprolol) all protonate to net charge +1 at their correct basic nitrogen (quinidine correctly on the aliphatic quinuclidine N, not the weakly-basic quinoline N); two acids (diclofenac, warfarin) both deprotonate to −1. **Part 2**: zero compute failures across all 5,655 compounds (4,905 train + 750 blind), zero RDKit warnings. Training-set mean net charge **0.374** vs. Kiani's reported 0.67 (below their stated 0.4–0.9 "consistent" band but far from the near-zero value a broken/no-op protonation would produce — Part 1 already rules that out directly, and 55.7% of training compounds carry nonzero charge) — reported as a population/tool difference (Dimorphite-DL vs. FAF-Drugs4), not tuned toward their number. Neutral-vs-protonated comparison (training set only, per task scope) quantifies the exact risk this check exists to guard against: 2,716/4,905 (55.4%) of compounds would have a different charge value under a silent RDKit-only fallback, whose neutral-SMILES column is 99.6% zero. Saved to `outputs/17_protonation_charge_check/charge_logp_features.csv` (reusable, kept separate from model-specific output; `net_charge_neutral` populated for training rows only, `NaN` for blind rows, matching the task's explicit training-only scope for that column). **Part 4 criteria frozen to `outputs/17_protonation_charge_check/decision_criteria.json` before Part 3's results were computed** (PASS: `|Spearman| >= 0.10` on ≥1 isoform OR median pIC50 diff between +1/0 charge groups `>= 0.3` with both groups `n >= 100`; MECHANISM CONFIRMED: CYP2D6 positive AND CYP2C9 negative, both clearing the PASS bar). **Part 3/verdict result, per isoform, no cross-isoform averaging**: CYP1A2 PASS (rho=−0.2047, the *largest*-magnitude of any isoform — not the a-priori expectation of "little signal"); CYP2C9 PASS (rho=−0.1535, direction as predicted); CYP2D6 PASS (rho=+0.1691, direction as predicted); CYP3A4 FAIL (rho=−0.0712, below bar — matches the a-priori "little expected" prediction). None of the four clears the median-diff arm alone (closest: CYP1A2 at 0.294, just under 0.3) — every PASS is carried by the Spearman arm. **MECHANISM CONFIRMED: TRUE** per the frozen rule's letter. **A confound check (not pre-registered, added during interpretation) tempers this**: net charge and Crippen logP are anti-correlated in this population (rho=−0.10, p=7e-13) — where an isoform's own logP-pIC50 correlation is strongly positive (CYP1A2 +0.23, CYP2C9 +0.49), this alone predicts a negative charge-pIC50 correlation with no electrostatic mechanism required, so CYP1A2's and CYP2C9's PASS results are **not distinguishable from a lipophilicity confound**. CYP2D6 is the exception and the strongest single result: its own logP-pIC50 correlation is ~0 and non-significant (rho=+0.034, p=0.19), so its positive charge signal is **not** explained by the confound — the cleanest evidence in this project's own data for the mechanism this notebook set out to test. **Verdict for the gated decision**: overall PASS, but unevenly earned — CYP2D6 is the strongest candidate for carrying a charge descriptor into a future shared-descriptor Chemprop run (`X_d` = charge + logP proxy, concatenated to `chemprop_chemeleoninit`); CYP1A2/CYP2C9 clear the numerical bar but would need OOF ST-RAE evidence, not Spearman, before being trusted; CYP3A4 has no case on this evidence. COMPLETE — report only, no model built, no retraining, no submission; `data/folds/cv_folds.csv`, `outputs/05_cv_comparison/`, `outputs/board_solved_calibration/`, `src/calibration.py` and `src/features.py` all untouched. **Addendum appended to the end of this same notebook, same day**: quantifies the charge/logP confound via Spearman **partial** correlation (charge vs. pIC50, controlling for `crippen_logp`; standard first-order formula computed directly, no new package), replacing the qualitative "not distinguishable from" hedge above with a number. Interpretation pre-registered *before* computing anything and appended as a new, clearly-labelled `partial_correlation_confound_check` block in the existing `decision_criteria.json` (Part 3's original block read back and confirmed unmodified): CONFOUND-DRIVEN if an isoform's partial `|rho|` drops below the same 0.10 bar having cleared it unconditionally, SURVIVES if it stays at/above 0.10 with the same sign; CYP2D6 pre-registered to SURVIVE (its logP-pIC50 correlation is null), CYP1A2/CYP2C9 pre-registered as genuinely uncertain, CYP3A4 excluded from labelling (already FAILED Part 3's unconditional bar, reported for completeness only). **Result, real and somewhat surprising relative to Part 3's own hedge: all three PASS isoforms SURVIVE.** CYP1A2 -0.2047→-0.1931 (confound explains only ~6% of the raw correlation's magnitude — much less than the qualitative hedge implied); CYP2C9 -0.1535→-0.1204 (~22% explained, a real but non-decisive dent); CYP2D6 +0.1691→+0.1772 (partial is if anything *larger*, since logP carries nothing to confound with). CYP3A4, reported for completeness only and not re-adjudicated: -0.0712→-0.0029, a ~96% collapse — consistent with its already-sub-threshold raw signal being almost entirely a lipophilicity artifact. Per the task's own framing: **Part 3's frozen PASS verdicts for CYP1A2/CYP2C9/CYP2D6 are not retracted** (they were correct against the criteria frozen at the time, which had no confound control) — what changes is the interpretation carried forward, now leaning toward all three isoforms' charge signals being largely real rather than logP-proxy artifacts. Effect-size caution restated explicitly: CYP2D6's partial rho (~0.177) is the same modest order of magnitude as its own +1-vs-0 median pIC50 gap (~0.17 log units, from Part 3's own table) against a real blind ST-RAE of 0.8299 (notebook 12) — genuine and independent of logP, but not large. Saved to a new file, `outputs/17_protonation_charge_check/partial_correlation_verdict.json`; `verdict_summary.json` and `charge_logp_features.csv` untouched (confirmed byte-identical row/column counts after the full-notebook re-execution this append required). No adoption or modelling recommendation made — still a separate, later decision left to the user.
- **18**: cheap screen only — single fold (`repeat_0`, `fold=0`), three seeds, no full 25-fold CV, no ensemble built, no submission. Trains a charge-augmented variant of `chemprop_chemeleoninit` (the existing model plus notebook 17's `net_charge_ph74` — NOT `crippen_logp`, kept single-variable and attributable — concatenated via Chemprop v2.3.1's native `--descriptors-columns` extra-datapoint-descriptor mechanism, confirmed against the installed CLI/source before writing training code: fits a `StandardScaler` on `screen_inner_train` rows only, excluding both `screen_inner_val` and the held-out fold, baked into the saved checkpoint and applied automatically at predict time — verified directly, chemprop's own logged scaler `loc=0.37, scale=0.839` matched an independent pandas computation on the same population to 3 decimals) against a plain BASELINE arm retrained on the identical fold/seeds/environment (not reused from `outputs/05_cv_comparison/`). **Baseline sanity check: exact match** (0.0000 abs ST-RAE difference on all 4 isoforms) between the retrained baseline at the one seed shared with `05`'s own `chemprop_chemeleoninit__repeat0_fold0` run (2684470948, drawn directly from that run's own manifest seed, not invented) and `05`'s stored numbers — strong confirmation the fold/seed/environment plumbing is correct. Seeds pre-registered before any training (`outputs/18_charge_augmented_chemprop_screen/prereg.json`): CYP3A4 as a within-experiment null control (its charge signal collapsed to -0.003 under 17's confound check), CYP1A2/CYP2C9/CYP2D6 as plausible-gain isoforms. **CYP3A4 control passed as pre-registered** (moved by 0.0144 ST-RAE, well inside its own 0.0313 combined-seed spread — unresolved, consistent with there being nothing to learn from the column on this isoform) — plumbing trusted, proceeded to interpret the other three. **Question A (solo ST-RAE, mean±spread across 3 seeds): a clean tie on all four isoforms** — CYP1A2 (+0.0131 vs. spread 0.0150), CYP2C9 (+0.0007 vs. 0.0158), CYP2D6 (+0.0038 vs. 0.0626), CYP3A4 (-0.0144 vs. 0.0313) — every difference smaller than its own within-arm seed spread, none resolved in either direction, including the three isoforms notebook 17 flagged as plausible. **Question B (decorrelation) required a correction caught during this notebook's own review, not silently smoothed over**: the pre-registered reference ("existing pool members sit around 0.88-0.93 pairwise OOF correlation") does not match what this single fold's ~277-450-compound held-out set actually shows for the 11-config pool's own internal pairwise correlation (0.50-0.77, `outputs/18_charge_augmented_chemprop_screen/decorrelation_summary.csv`) — plausibly because notebook 11b's own reference figures were residual (not raw-prediction) correlation pooled across all 25 folds, not a single fold's raw predictions; stated as a plausible, not confirmed, account. An initial verdict cell that compared the charge arm's pool correlation against the mismatched absolute 0.85 cutoff wrongly called every isoform "decorrelated"; corrected in place (markdown explaining the discrepancy retained directly in the notebook) to the apples-to-apples relative comparison instead — charge-arm-vs-pool mean correlation against the pool's own internal mean, both computed on the same fold. **Corrected result: charge-vs-pool correlation sits slightly ABOVE the pool's own internal mean on all four isoforms** (e.g. CYP1A2 0.667 vs. pool's own 0.564; CYP3A4 0.814 vs. 0.771) — pool-typical, not decorrelated — and the charge arm's error correlation with its own baseline specifically is 0.984-0.990, about what two independently-seeded instances of the same plain recipe would show. **Verdict: (c) on all four isoforms — solo ties AND correlated at pool-typical levels; the charge signal notebook 17 established does not appear to be reaching this model's output in a way that matters, via this one mechanism.** No isoform is a candidate for the full 25-fold CV comparison on this screen's evidence. Training/predict CSV-builders and a predict-CLI wrapper for the extra descriptor column were written locally in the notebook (not added to `src/chemprop_screen.py`, which stays unmodified) since that module's existing `build_training_csv`/`build_predict_csv`/`run_chemprop_predict` have no hook for an extra column; `run_chemprop_train`'s existing `architecture_args` list already accepted the extra `--descriptors-columns` flag with zero modification needed. A cache-skip (`PRED_DIR/{tag}.csv` existence check, matching `scripts/run_5x5_cv_comparison.py`'s own `is_done()` convention) was added to the local training wrapper so the Part 4 correction above could be applied and the whole notebook re-executed in ~18s instead of redoing the original ~50 minutes of real training. COMPLETE — report only, no model built beyond the screen itself, no ensemble, no submission; `data/folds/cv_folds.csv`, `outputs/05_cv_comparison/`, `outputs/11_caruana_prep/`, `outputs/17_protonation_charge_check/`, `src/calibration.py`, `src/chemprop_screen.py` and every existing submission artifact untouched. **Unrelated note**: a concurrent session built `notebooks/19_cyp2c9_revert_submission_candidate.ipynb` / `outputs/19_cyp2c9_revert/` during this notebook's own background training run (its own pending CLAUDE.md entry sits at `outputs/19_cyp2c9_revert/CLAUDE_md_entry_pending.md`, not yet added here — left untouched, not this notebook's work, and not added to this log by notebook 18).
- **19**: CYP2C9 revert, built directly from NB16's real board result (2026-09-14 capture: CYP1A2 0.6954 / CYP2C9 0.5770 / CYP2D6 0.8299 / CYP3A4 0.4434, macro 0.6364 -- not yet in docs/leaderboard_submissions.md as of this notebook). NB16's board-solved recentring was a clear win for CYP2D6 (ST-RAE 1.1673 -> 0.8299, R2 -0.5941 -> +0.2713, Spearman unchanged at 0.4000 -- confirming a pure placement fix) but a regression for CYP2C9 (0.5489 [10c] / 0.5375 [NB12, best on record] -> 0.5770; R2 0.5272 -> 0.5133) -- consistent with the recentring earning its place only where placement was genuinely broken (large b_mag, negative R2) and costing performance where it was already close to adequate (CYP2C9's b_mag was the smallest well-determined solve in NB16). Pure recombination, no retraining/re-solving/new calibration: CYP1A2/CYP2D6/CYP3A4 taken byte-identical (sha256-confirmed) from NB16's own submitted file (outputs/board_solved_calibration/submission_candidate.csv, read-only); CYP2C9 reverted to NB12's own uncalibrated, capped-Caruana-ensemble column (outputs/12_caruana_retrain_predict/submission_candidate.csv, read-only, real ST-RAE 0.5375). Written to outputs/19_cyp2c9_revert/submission_candidate.csv (new directory -- outputs/board_solved_calibration/ untouched). Validated PASS via the unmodified validate_activity_submission. A gradio_client submission cell is prepared, fully commented out, and NOT executed -- no adoption/submission decision made, left to the user. COMPLETE, no retraining anywhere, no existing output file touched. Built under a concurrency constraint (another notebook run in parallel) -- pandas/file IO only, no torch/chemprop import.
- **20**: cheap screen only — single fold (`repeat_0`, `fold=0`, same fold as `06`/`18`), three seeds, no full 25-fold CV, no ensemble, no submission. Tests notebook 06's residual-based CYP2D6 exclusion finding (CV ST-RAE 0.937→0.897, p=1.6e-05 for `chemprop_chemeleoninit`) as an isolated, single-variable change — **never previously tested on its own**, since it was folded into `07`'s tuning and then `08`'s ensembling before either NB10 or NB12 was submitted. Three arms, same fold/seeds/architecture/epochs(50)/patience(5): BASELINE (no exclusion, retrained here rather than reused from `05`), RESIDUAL (`06`'s `criterion1_residual_flagged.csv` — read-only, not recomputed — masked to NaN in CYP2D6 only), RANDOM (a same-count, disjoint-by-construction random draw from this fold's other CYP2D6-labeled training rows, fixed seed `20260914`, distinct from the three training seeds and reused identically across all three). RANDOM's purpose: separate a genuine data-quality effect from `06`'s own leading, never-verified hypothesis that chemprop's multitask loss reweights by valid-label count, which `06` proposed but could not test (its own r=0.92-0.95 cross-isoform-divergence finding was consistent with either explanation). Interpretation frozen in `outputs/20_residual_exclusion_screen/prereg.json` before any training, including a stated 70%-of-magnitude threshold separating REWEIGHTING_ARTIFACT from MIXED. **Provenance/Part 0, reproducing `06`'s own numbers exactly**: 59 of the 75 globally-flagged compounds fall in this fold's training portion (16 fall in this fold's own held-out set, correctly never masked), CYP2D6 training labels 1198→1139 after masking; all four byte-identical checks (row order/count, split assignment, other-3-isoform columns, NaN pattern) pass at every seed for both masked arms. **Baseline sanity check: exact match** (0.0000 abs ST-RAE, all 4 isoforms) against `05`'s stored `chemprop_chemeleoninit__repeat0_fold0` numbers. First run took 5.55 min; all 9 runs completed in ~73 min total wall time across two `nbconvert` invocations — the first hit a 1800s single-cell timeout partway through the batched "remaining 8 runs" cell (4 of 9 runs done: BASELINE×3, RESIDUAL×1), resumed cleanly with a larger per-cell timeout via this project's own on-disk cache-skip (`PRED_DIR/{tag}.csv`) contract, no work lost or redone — same pattern as `05c`'s precedent. **Question A result: NULL.** CYP2D6 BASELINE 0.9599±0.0626 (3-seed std), RESIDUAL 0.9362±0.0249 (diff −0.0238, unresolved — smaller than BASELINE's own seed spread), RANDOM 0.9639±0.0601 (diff +0.0040, unresolved) — neither arm clears the frozen resolution threshold; `06`'s CV effect does not reproduce on this single fold under this protocol. Important caveat stated plainly, not glossed over: BASELINE's own CYP2D6 3-seed spread (0.0626) is comparable in size to `06`'s entire original 25-fold CV effect (0.040) — a single fold with 3 seeds has limited power to resolve an effect this size, so NULL here is not strong evidence the effect is fake, only that this screen's design couldn't resolve it either way. One incidental finding on the side: CYP3A4 (labels untouched by any arm) shows a RESOLVED "RESIDUAL improves" reading (diff −0.0349) — direct evidence that a same-seed masked run can move an untouched isoform's *score*, not just its raw predictions, consistent with Question B below. **Question B result: real, and the strongest evidence yet for the reweighting mechanism.** RESIDUAL and RANDOM — two disjoint 59-compound masked sets — produce nearly indistinguishable same-seed divergence from BASELINE on all three untouched isoforms (Pearson r vs. BASELINE: CYP1A2 0.9511 vs. 0.9543, CYP2C9 0.9679 vs. 0.9699, CYP3A4 0.9686 vs. 0.9680 — gaps of 0.002–0.006, tighter even than `06`'s own residual-vs-CI-width gap of 0.005–0.013 on a different pair of disjoint masked sets). A secondary, unanticipated finding surfaced while building the pre-registered "noise floor" reference: BASELINE-seed-vs-BASELINE-seed correlation (different training seed, zero masking: 0.87–0.91) is *lower* — i.e. more divergent — than either masked arm's same-seed-vs-BASELINE correlation (0.95–0.97), meaning seed choice alone perturbs CYP1A2/CYP2C9/CYP3A4 predictions more than an isolated 59-compound (1.5% of training rows) CYP2D6 mask does; read alongside `06`'s own determinism finding (identical seed + zero masking = bit-identical, r=1.0 exactly), the correct floor for judging the masked arms is 1.0, not this cross-seed figure, so the observed 0.95–0.97 same-seed divergence is real and attributable to masking, just smaller in magnitude than seed-driven variability. **Verdict: CYP2D6's own score is NULL on this screen (not currently a candidate for the full 25-fold comparison on this evidence alone), but the cross-isoform result independently and strongly supports the reweighting-artifact mechanism over pure noise** — exactly the kind of split outcome the pre-registration anticipated as possible. No submission recommendation made (none was in scope). `data/folds/cv_folds.csv`, `outputs/05_cv_comparison/`, `outputs/06_outlier_check/`, `outputs/11_caruana_prep/`, `outputs/board_solved_calibration/`, `outputs/19_cyp2c9_revert/`, `src/calibration.py`, `src/chemprop_screen.py` and every existing submission artifact confirmed untouched (`git status` after the run).
- **21**: read-only analysis, no training/Chemprop/torch/submission/calibration -- derives the ST-RAE-optimal uniform additive offset per isoform directly from this project's own pooled OOF predictions (`chemprop_chemeleoninit`, `outputs/11_caruana_prep/oof_long_{iso}.csv`, not recomputed) and the training credible intervals (`conf_low`/`conf_high`, joined fresh from `train_inhibition_curated.csv`, row counts and null-check asserted, cross-checked against `oof_long`'s own stored bounds -- identical). Motivated by a comparable real entrant reporting their own ST-RAE optimum was found by sampling placements against the live board rather than derived, since low-activity compounds carry wide credible intervals (over-prediction cheap, under-prediction punished) -- this project already has the interval widths, so the optimum is derived locally instead. **A real quoted-figure discrepancy was caught and corrected during this build, per this project's own verification convention**: the task brief's stated CYP2D6 slope/intercept (0.870262, 1.906567) belong to notebook 16's ORIGINAL (superseded) correction (target mean ~5.83), not the OVERRIDE notebook 16 actually deployed to `outputs/board_solved_calibration/submission_candidate.csv` (verified directly against notebook 16's own cell 32 output: slope=0.868575, intercept=-0.760352, corrected std=0.557002) -- the brief's quoted mean-shift figure (4.5087 -> 3.1558, equivalent offset -1.3529) *is* the deployed override's own number and was used as-is; only the slope/intercept pairing was mismatched. **Part 0**: interval widths correlate strongly negatively with pIC50 on all four isoforms (Spearman -0.56 to -0.93) -- wider intervals concentrate at low pIC50, the necessary precondition for the claimed asymmetry, confirmed quantitatively not assumed. Zero `conf_high < conf_low` violations. **Part 1** (401-point sweep, -2.00 to +2.00 pIC50 units, step 0.01, vendored `rae_soft_threshold_absolute_error` and `r2_score` only, Spearman invariance asserted and confirmed exact across the whole sweep, grid-search R2 optimum cross-checked against the closed-form mean-residual-zero optimum and matched within grid resolution on all four isoforms): **a real, small, consistently-signed gap on all four isoforms** -- ST-RAE optimum sits ABOVE (predicts higher than) the R2 optimum everywhere, by +0.06 (CYP2D6) to +0.12 (CYP3A4) pIC50 units; ST-RAE improvement from perfect OOF placement over raw is correspondingly modest (~0.006-0.022 ST-RAE units). **Part 2**: mapping NB16's deployed CYP2D6 equivalent shift (-1.3529) onto this OOF curve lands at ST-RAE 3.08 -- roughly 3x the raw baseline and far from this curve's own optimum (+0.09, ST-RAE 0.99) -- a striking, direct illustration of the stated caveat rather than evidence the deployed correction was wrong: it targeted the *blind* population's solved centre (notebook 16), a different, lower-centred population than this OOF curve's training-population evidence (consistent with notebook 17's charge-population-shift finding), so an OOF-optimal offset is a hypothesis about the blind population, not a measurement of it. Half-set live-leaderboard scoring noted as an added caveat, not acted on. **Part 3**: the claimed asymmetry mechanism is CONFIRMED directly on all four isoforms, not assumed -- low-pIC50 halves carry 2.9-7.0x wider mean intervals than high-pIC50 halves, and total under-prediction soft-threshold error exceeds total over-prediction error by 1.65-2.79x at raw placement, on every isoform. **Part 5 verdict**: the trained-objective-vs-scored-metric mismatch is real and confirmed, but its pure-offset cost is small (0.06-0.12 pIC50 units) -- nowhere near the scale of the population-centre mismatch that actually drove CYP2D6's real board improvement. Does not itself justify an interval-aware training objective on the evidence gathered (a uniform offset is a weak proxy for what such an objective could do, and this curve can't speak beyond offsets) -- the bigger lever remains population-centre placement, notebook 16's own mechanism. Figures (`outputs/21_strae_offset_curve/figures/`): `strae_offset_curve.png` (ST-RAE vs. offset, optima marked, CYP2D6 panel also marks the deployed equivalent shift), `strae_vs_r2_offset.png` (twin-axis ST-RAE/R2 divergence, the figure carrying the argument), `interval_width_vs_pic50.png` (width vs. pIC50, de-duplicated to unique compounds, median-split means marked). Sweep saved to `offset_curve.csv`. COMPLETE -- report only, no offset applied to blind predictions, no submission built or recommended; `data/folds/cv_folds.csv`, `outputs/05_cv_comparison/`, `outputs/11_caruana_prep/`, `outputs/board_solved_calibration/`, `outputs/19_cyp2c9_revert/`, `outputs/20_residual_exclusion_screen/`, and `src/` all untouched. Built under a concurrency constraint (notebook 20's Chemprop training running in parallel) -- pandas/numpy/matplotlib/scipy/sklearn and the vendored scorer only, no torch/chemprop import.
- **22**: cheap screen only -- single fold (`repeat_0`, `fold=0`, same fold as notebooks 04a/06/18/20), three seeds, no full 25-fold CV, no ensemble, no submission. Tests whether adding auxiliary prediction heads from this project's own already-released challenge files (`TRAIN_TDI.csv`, `TRAIN_Emax.csv`) -- an ADDING-information mechanism, unlike every prior screen (ensembling, charge descriptors, interval-aware objectives, residual exclusion), all of which rearranged existing information. Motivated by the leading entrant's per-assay-head training on ~31,670 compounds/161,400 labels vs. this project's 4,905 compounds/6,525 measurements. Three arms, same fold/seeds/architecture(chemprop_chemeleoninit)/epochs(50)/patience(5): BASELINE (4 heads, retrained here rather than reused from `05`), TDI (+4 `pIC50_TDI_condition` heads), TDI+EMAX (+4 `EmaxVsPosCtrl_direct_inhibition` heads). Regression heads only -- `*_is_TDI` (categorical) and all `_conf_high`/`_conf_low`/`_std` columns excluded; the long-format single-concentration screen explicitly out of scope (would need a pivot + a replicate-aggregation judgement call). **Part 0 leakage gate (run first): PASSED, no exclusion needed** -- checked by inchikey (via `src/features.py`'s existing canonicalisation, not raw SMILES or Molecule_Name), ALL compounds in both TRAIN_TDI.csv and TRAIN_Emax.csv against `test_blinded_curated.csv`'s 750 compounds: zero overlap in either file. **Key structural finding, discovered while building the training tables, not assumed**: of TRAIN_TDI.csv's 1,240 TDI-only compounds (present in TRAIN_TDI.csv, absent from `train_inhibition_curated.csv` -- notebook 00's own finding), each contributes exactly one non-null TDI_condition label, and that contribution is almost entirely CYP3A4 (1,238 of 1,240) with 2 in CYP2D6 and ZERO in CYP1A2/CYP2C9 -- the pool-expansion mechanism this notebook tests is real for CYP3A4 only (53% pool growth relative to CYP3A4's own scored-label count) and essentially absent for the other three isoforms. A second finding: TRAIN_Emax.csv's `EmaxVsPosCtrl_direct_inhibition` column's non-null pattern is an EXACT match to the scored `direct_inhibition` label's own non-null pattern on every isoform (confirmed, not assumed) -- the Emax head adds zero new compound-pool coverage of its own; it only adds a same-population extra column on top of TDI's contribution. Fold assignment (stated explicitly, confirmed with the user's own task framing): the extra compounds are never added to `cv_folds.csv` and always assigned to the training portion (never held out); within the training portion they are placed in `screen_inner_train` only (a narrower decision, documented in-notebook), keeping the early-stopping validation set's composition identical across all three arms. Held-out portion confirmed IDENTICAL across all 3 arms and all 3 seeds; the 4 scored target columns confirmed byte-identical (tolerance 1e-8) between BASELINE and the augmented population for every shared compound. Pre-registered in `outputs/22_auxiliary_heads_screen/prereg.json` before training: IMPROVES/TIES/HURTS resolution rule uses BASELINE's own 3-seed ST-RAE spread; CYP3A4 predicted to benefit most (only isoform with a non-trivial pool-expansion ratio), CYP1A2/CYP2C9/CYP2D6 predicted near-ties; dose-response expectation (TDI+EMAX >= TDI) stated before training. **Baseline sanity check against `05`'s stored `chemprop_chemeleoninit__repeat0_fold0` numbers: exact match, 0.0000 max abs ST-RAE diff on all four isoforms** (matching notebooks 18/20's own precedent for this check). Training took 85.6 min total across the 9 runs (BASELINE ~5.1-11.6 min/run; TDI/TDI+EMAX 7.5-22.1 min/run, consistent with the ~25%-larger training pool, one TDI run at 22.1 min ran markedly longer than its own two same-arm reruns for reasons not investigated further). **Verdict: a real, mixed result -- neither the PXR negative precedent nor a clean positive story, reported per isoform rather than forced into one.** CYP3A4 (the only isoform predicted to show the pool-expansion mechanism, at 65.7% new-label growth relative to its own scored-label count) IMPROVES in both TDI (0.5223->0.4809, resolved) and TDI+EMAX (0.5223->0.4565, resolved) -- the ordering check's top prediction matched the top observed result exactly. CYP2C9 IMPROVES in TDI (0.7142->0.6896, resolved) despite a predicted-zero pool-expansion contribution (0 TDI-only compounds carry a CYP2C9 label) -- a real gain not explained by the tested mechanism, reported honestly as unpredicted rather than retrofitted to the hypothesis; TDI+EMAX washes this back out to a TIE (0.7142->0.7222). CYP1A2 TIES in TDI (0.7995->0.7979) but HURTS in TDI+EMAX (0.7995->0.8494, resolved regression) -- a genuine, resolved head-dilution effect, the dose-response expectation (TDI+EMAX >= TDI) is VIOLATED here specifically (diff +0.0516 vs. combined spread 0.0510). CYP2D6 TIES in both arms (read with notebook 21's own OOF-unreliability caveat attached, not taken at face value). Held-out error correlation vs. BASELINE, per isoform, sits ABOVE the BASELINE-seed-vs-seed noise floor for every arm on every isoform (e.g. CYP3A4: floor 0.870, TDI 0.901, TDI+EMAX 0.907) -- the auxiliary heads did not destabilise predictions beyond ordinary seed-to-seed variation; if anything each arm tracks BASELINE slightly more closely than two independently-seeded BASELINE runs track each other. **CYP2C9 and CYP3A4 are candidates for the full 25-fold CV comparison on this screen's evidence (NOT run here)**; CYP1A2/CYP2D6 are not. Figures (`outputs/22_auxiliary_heads_screen/figures/`): `strae_by_arm_isoform.png`, `auxiliary_label_counts.png` (visualises the CYP3A4-only pool-expansion asymmetry), `error_correlation_heatmap.png`. Per-run scores in `run_scores.csv`. `data/folds/cv_folds.csv`, `data/processed/*`, `outputs/05_cv_comparison/`, `outputs/06_outlier_check/`, `outputs/11_caruana_prep/`, `outputs/board_solved_calibration/`, `outputs/19_cyp2c9_revert/`, `outputs/20_residual_exclusion_screen/`, `outputs/21_strae_offset_curve/`, `src/calibration.py` and every existing submission artifact confirmed untouched (`git status` after the run). No submission recommendation made.
- **23**: read-only counting exercise, no training/Chemprop/torch/submission -- quantifies how many NEW compounds (by inchikey, via `src/features.py`'s existing canonicalisation, never raw SMILES/Molecule_Name) each of this project's own unused challenge files would add to each isoform's training pool, to decide whether `cyp-challenge-single-concentration-TRAIN.csv` (17,504 rows, 4,376 compounds, long format, never previously checked for compound overlap) is a larger, four-isoform version of notebook 22's TDI pool-expansion mechanism. **Part 1**: curated file's per-isoform scored counts (CYP1A2 1,412 / CYP2C9 1,285 / CYP2D6 1,493 / CYP3A4 2,335, 4,905 unique compounds) confirmed to match notebook 00's reference exactly. **Part 2, the core result**: the single-concentration file's entire 4,375-unique-compound set (one inchikey collision found: two distinct Molecule_Names, E/Z-ambiguous SMILES, same structure) is a STRICT SUBSET of the curated 4,905-compound pool -- confirmed directly, not inferred -- contributing exactly ZERO new compounds to all four isoforms (CYP1A2/CYP2C9/CYP2D6/CYP3A4 all 0). File is genuinely single-concentration (one distinct `concentration_M` value across all 17,504 rows); the 4 (compound, enzyme) pairs with 2 rows instead of 1 are fully explained by the one inchikey collision, not a real replicate-aggregation question. Zero overlap with the 750-compound blind set. **Part 3**: TDI reproduced notebook 22's own quoted figures exactly (0/0/2/1238 new compounds, CYP1A2/CYP2C9/CYP2D6/CYP3A4) -- verified against notebook 22's own saved cell output before use, no discrepancy. **A real, small discrepancy found and flagged for Emax**: recomputed directly from `TRAIN_Emax.csv`'s own 6,146-row population (rather than reused from notebook 22's TDI-anchored 6,145-row population), Emax contributes exactly 1 new compound each to CYP1A2 and CYP2D6 (one compound, `OCNT-2329004`, sitting outside both curated AND TDI's own extra pool -- structurally invisible to notebook 22's own check, which built Emax's columns onto TDI's population via a left join). Notebook 22's "Emax adds zero new compounds" claim was correct within the population it checked but not literally true of the raw file taken alone; magnitude negligible (1 compound on a 1,412-1,493 pool), changes no conclusion. **Part 4 verdict**: material defined as new-compound count >= 20% of that isoform's existing scored pool (matching TDI/CYP3A4's own 53% precedent) -- only TDI/CYP3A4 (53.0%) clears this bar of all 12 (file x isoform) combinations checked. The single-concentration file reaches NONE of the four isoforms with new compounds, including CYP3A4 -- it does not reach the three isoforms TDI's mechanism missed, and compound-pool expansion from this project's own released files is, on this evidence, largely exhausted. CYP3A4's existing TDI result is unaffected by anything found here and remains a 25-fold confirmation candidate on its own evidence. No training run, ensemble, or submission built or recommended; whether new compounds (TDI/CYP3A4) actually improve a model is what notebook 22's screen already tested, not re-tested here. Figures (`outputs/23_compound_pool_overlap/figures/`): `new_compounds_by_file_isoform.png` (grouped bars, new-compound counts per isoform x file, scored-pool size marked as reference), `pool_overlap_venn_or_upset.png` (stacked bars, per isoform, overlap structure vs. curated pool -- already-scored/curated-unscored/new). Combined count table saved to `outputs/23_compound_pool_overlap/pool_overlap_counts.csv`. COMPLETE -- report only; `data/`, `outputs/05_cv_comparison/`, `outputs/11_caruana_prep/`, `outputs/22_auxiliary_heads_screen/`, and `src/` all untouched (only a new `outputs/23_compound_pool_overlap/` directory created).
- **24**: read-only analysis, no training/Chemprop/torch/submission -- refits the ensemble stacker on `outputs/11_caruana_prep/oof_long_{iso}.csv`'s existing pooled OOF predictions (all 11 real configs, unmodified) with NON-NEGATIVE combiners (NNLS, non-negative ridge via augmented-design NNLS), compared against unconstrained ridge/elastic-net, notebook 08's plain simple average, and notebook 11b's capped Caruana selection. Motivated by a top-10 entrant on this exact challenge reporting the same CV-wins/blind-loses failure mode this project hit with NB10/NB12, and naming unconstrained-combiner coefficient cancellation as the structural cause. **Zero nulls found in any of the 4 isoform tables** (7,060/6,425/7,465/11,675 rows respectively, all 25 (repeat,fold) groups present and complete) -- no null-handling policy needed. **Honest nested design, stated as this notebook's central methodological point**: notebook 11b's own Caruana selection fits weights on the full pooled 25-fold OOF table and scores those same folds with the fixed result -- the exact leakage trap this project's CLAUDE.md already flags (Caruana et al. 2004). This notebook instead uses the existing 25 `(repeat, fold)` groups as a genuine outer loop (fit on the other 24 groups, predict the held-out one) for every fitted combiner, with penalty strength selected by an inner `GroupKFold(n_splits=4)` split on the 24 training groups only; `caruana_capped`'s recorded numbers are reported for reference but excluded from the formal repeated-measures test since its protocol differs. **Correlation regime, checked not assumed**: raw-prediction correlation (mean 0.467-0.757 across isoforms) sits mostly below the cited entrant's 0.55-0.88 band, but error correlation -- the quantity that actually determines whether combination helps (Dietterich 2000) -- runs 0.792-0.908 (up to 0.965 on individual pairs), at or above the *top* of that band on every isoform and above notebook 18's own single-fold 0.50-0.77 estimate; error correlation exceeds raw-prediction correlation on every isoform, consistent with notebook 11b's own ~0.79-0.96 residual-correlation finding on this same pool. **Cancellation check: a clean negative result.** No unconstrained (ridge/elastic-net) mean coefficient, on any isoform, is more negative than -0.053 or exceeds 1.5 in magnitude in either direction -- nowhere near the cited entrant's own example (+1.9 against -0.6); the handful of small negative weights that do appear sit on this pool's weakest tabular members (`mordred_pca__*`, `*__xgboost`), consistent with ordinary shrinkage rather than cancellation. **The specific structural cause the cited entrant diagnosed for their own pool does not appear in this project's own 11-config pool.** NNLS/non-negative-ridge weights concentrate on 4-8 of 11 members per isoform (never all 11), heavily on `chemprop_chemeleoninit` plus a few tabular configs, closely tracking notebook 11b's own Caruana selections; NNLS coefficients sum to ~1.0 on every isoform without being constrained to. **OOF ST-RAE result (Levene homogeneous on all 4 isoforms -- ST-RAE went parametric here, unlike notebook 05's own 11-config comparison, checked not assumed; RM-ANOVA + Tukey HSD used throughout)**: every fitted combiner beats the best single config on every isoform (7-11% relative improvement, exceeding fold-to-fold variability, Tukey-significant); non-negative combiners (NNLS/NN-ridge) are the best- or joint-best-performing fitted combiner on every isoform, never worse than unconstrained ridge/elastic-net. Against `caruana_capped`'s own (differently-protocoled, optimistic) numbers: honest non-negative stacking is close but slightly behind on CYP1A2/CYP2C9/CYP2D6, and *beats* it on CYP3A4 (0.4883 vs. 0.4964). **CYP2D6 caveat repeated explicitly**: notebook 21 established OOF ST-RAE is not a reliable ranking signal for CYP2D6 (the deployed blind-targeted recentring that took the real board from 1.1673 to 0.8299 scores 3.08 on this project's own OOF curve) -- CYP2D6's numbers here carry that caveat undiminished. Figures (`outputs/24_nonnegative_stacking/figures/`): `combiner_comparison_strae.png`, `stacker_coefficients.png` (unconstrained vs. non-negative, per isoform, mean+/-std over 25 outer folds), `error_correlation_matrix.png`. Scores/coefficients saved to `combiner_scores.csv`/`coefficients.csv` for reproducibility. COMPLETE -- report only, no adoption recommendation made; `outputs/11_caruana_prep/`, `outputs/11b_caruana_selection/`, `outputs/05_cv_comparison/`, `outputs/08_ensemble_selection/`, `outputs/board_solved_calibration/`, `outputs/19_cyp2c9_revert/`, `data/folds/cv_folds.csv`, `src/` and every submission artifact confirmed untouched (`git status` after the run).
- **25**: read-only diagnostic, no training/Chemprop/torch/submission, no external corpus retrieved -- characterises the compounds every one of the 11 configs gets wrong, using `outputs/11_caruana_prep/oof_long_{iso}.csv`'s existing pooled OOF predictions (unmodified), to test whether the planned retrieved-neighbour-corpus pretraining step is aimed at the right population before building it. Motivated directly by notebook 24's finding (error correlation 0.79-0.91 against raw-prediction correlation of only 0.47-0.76) that this pool's models share failures rather than diversifying them, and by the reframing that gives notebook 18's own 0.984-0.990 error-correlation result a more likely explanation (a property of the pool, not evidence the charge feature failed to reach the output). **Zero nulls, every compound has exactly 5 repeat rows, y_true/conf_low/conf_high confirmed constant per compound** across all 4 isoform tables -- repeats averaged per (compound, config) before any hard/easy definition, within-compound across-repeat SD reported as the noise floor (pooled mean 0.10-0.16 pIC50 units across isoforms, comparable in scale to several of the physicochemical effect sizes found below). **Definitions frozen to `definitions.json` before any chemistry was computed** (no fingerprint, RDKit descriptor, or scaffold call anywhere above that point): CONSENSUS-HARD/EASY = worst/best decile by mean absolute error across all 11 configs; DISAGREEMENT = worst decile by cross-config SD of signed error (decile convention extended to this set as this notebook's own explicit choice, not specified in the brief); soft-threshold error computed via the vendored evaluator's own per-point clipped-error formula (`_resolve_bounds` imported directly, the two `np.clip` lines reproduced verbatim from `rae_soft_threshold_absolute_error`'s source -- not a reimplementation, and the only way to get a per-compound reading since the aggregate metric's baseline denominator collapses to zero for n=1). **Plain-AE vs. soft-threshold hard/easy sets differ materially on every isoform** (Jaccard overlap 0.06-0.48, all below the stated 0.8 materiality bar) -- soft-threshold carried forward as primary for Parts 2-4 as the brief specifies, and this is a real difference, not a formality: a compound's absolute error and its soft-threshold error diverge sharply whenever its credible interval is unusually wide or narrow. **Part 2 result, Mann-Whitney + rank-biserial effect size, Benjamini-Hochberg per isoform (10 continuous properties + qualifying scaffold-enrichment tests, "material" = BH-significant AND |r|>=0.3, or >=3 hard-set members for a scaffold)**: nearest-neighbour Tanimoto similarity to training (leave-one-out, `src/features.py`'s existing `ecfp4_fingerprints`/`nearest_neighbor_similarity`, reused not reimplemented) does **NOT** separate hard from easy on ANY isoform (|r| 0.02-0.09, none BH-significant) -- a clean, uniform negative across all four isoforms. What IS material on CYP2C9/CYP2D6/CYP3A4 (CYP1A2 has nothing material at all -- its hard set is genuinely diffuse on every property tested): higher measured pIC50 (more potent), narrower credible intervals (a mechanical property of how "hard" is defined under soft-threshold scoring, not necessarily a modelling failure), higher Crippen logP, lower FractionCSP3 (more rigid/aromatic); CYP3A4 additionally shows lower TPSA, higher heavy-atom count, and a small but real BH-significant enrichment of acyclic (ring-free, empty Bemis-Murcko scaffold) compounds in its hard set (4 of 6 such compounds, odds ratio 18.3). Scaffold enrichment otherwise found nothing -- consistent with notebook 02's 96.4%-singleton finding, checked fresh per isoform rather than re-quoted. **Part 3 (cross-isoform transfer, 6 isoform pairs, one BH family, phi coefficient + Fisher's exact)**: 3 of 6 pairs show a significant hard/hard association for compounds shared between them (CYP1A2-CYP2C9 phi=0.17, CYP1A2-CYP3A4 phi=0.13, CYP2C9-CYP3A4 phi=0.26) -- some compound-intrinsic difficulty exists, but it is partial, not universal (CYP2D6 pairs with none of the other three). **Part 4 verdict, per isoform, not averaged**: on all four isoforms, consensus-hard compounds are structurally ORDINARY relative to training, not distant -- a retrieved structural-neighbour corpus targeting "unseen chemistry" is aimed at the wrong problem as currently conceived, uniformly across isoforms, though the population it should instead target (potent, narrow-interval, lipophilic/rigid compounds) is real and isoform-dependent in strength. Figure 3 (`error_correlation_vs_difficulty.png`) makes the shared-ignorance mechanism visible directly: cross-config error SD correlates only weakly with error magnitude (Spearman rho 0.07-0.25), and the consensus-hard and DISAGREEMENT sets overlap only 11-19% -- hard compounds are mostly not the compounds models disagree about. Per-compound table saved to `outputs/25_hard_compound_analysis/hard_compound_table.csv`. COMPLETE -- diagnostic only, no model change recommended, no corpus built; `outputs/11_caruana_prep/`, `outputs/17_protonation_charge_check/`, `outputs/05_cv_comparison/`, `outputs/24_nonnegative_stacking/`, `data/`, `src/` and every submission artifact confirmed untouched (`git status` after the run).
- **26**: two read-only analyses, no training/Chemprop/torch/submission, no change to `src/calibration.py` -- (1) establishes the SIGN of notebook 25's consensus-hard-compound errors, testing whether spread compression (not representation) explains why hard compounds skew potent; (2) sweeps a pure SPREAD multiplier (mean held fixed) against `chemprop_chemeleoninit`'s OOF predictions, the first time this project has swept spread rather than offset, and compares the ST-RAE optimum to the `rho` shrinkage this project's deployed calibration actually applies. Several quoted figures verified directly against source notebooks before use, per this project's convention: notebook 21's 0.06-0.12 offset gap, 1.65-2.79x under/over-prediction ratio, and CYP2D6's 3.08 OOF ST-RAE at the deployed shift all confirmed exact; the "every prediction distribution sits below training-label spread" claim traced to notebook **10b** (not 05, a real misattribution in the task brief, corrected here) and confirmed (`std_nb10`/`std_04b` < `std_train` on 7 of 8 isoform x submission pairs, the 8th essentially tied at ratio 0.995); NB16's deployed CYP2D6 correction (rho=0.368531, target_std=1.511412, corrected_std=0.557002, ~2.7x narrower than its own target) reproduced exactly from that notebook's own cell 32 output. **User-confirmed decision before building Part 3** (the one item this task's own brief flagged as needing a stop, not a silent pick): Part 3's synthetic evaluation, which has no credible intervals to soft-threshold against, computes plain RAE via the same vendored function called with no bounds (its own docstring confirms this collapses the tolerance band to a point) -- no invented interval width, no new metric. **Part 1 rebuild verified against notebook 25's own saved values** (max abs diff <1e-8 on `mean_abs_error`, all isoforms) before trusting the newly-derived signed-error quantity notebook 25 never saved. **Result, uniform across all four isoforms, not averaged**: consensus-hard compounds ARE systematically under-predicted by this notebook's own pre-stated three-part rule (median signed error more negative than -0.05, >60% negative, BH-significant Fisher sign test vs. easy) -- frac_negative 0.64/0.86/0.77/0.93 for CYP1A2/CYP2C9/CYP2D6/CYP3A4, all BH-significant (Fisher odds ratios 2.6-52.2). A real nuance reported plainly, not smoothed over: CYP1A2's hard-set MEAN signed error is only -0.13 despite a strongly negative MEDIAN (-1.24), because roughly a third of its hard set is instead strongly OVER-predicted -- consensus-hard captures both tails of a bimodal error distribution, majority-but-not-exclusively under-prediction. The potency-decile trend (Figure 1) is a clean, textbook regression-to-the-mean signature on every isoform with exactly one sign crossing: strongly positive at the lowest-pIC50 decile (+1.11 to +1.88), strongly negative at the highest (-0.95 to -1.37). **Part 2 result (Spearman-invariance integrity check passed on the full [0.2, 3.0] sweep, all four isoforms; grid lower bound extended from the brief's stated 0.5 to 0.2 after the first run showed CYP2D6's own rho, 0.3685, sits below 0.5 -- would have silently clamped via `np.interp` rather than actually evaluating at rho, and CYP2D6's ST-RAE optimum in the original range landed exactly on that same boundary, a real signal not an artifact to paper over)**: the ST-RAE-optimal spread multiplier sits strictly between rho and 1.0 on every isoform -- CYP1A2 s=0.66 (rho=0.499), CYP2C9 s=0.82 (rho=0.654, but that rho was fit on `caruana_capped_ensemble` per `calibration_blind_population/params.csv`'s own `model` column, not `chemprop_chemeleoninit` -- flagged explicitly, not treated as equivalent to the other three isoforms), CYP2D6 s=0.48 (rho=0.369), CYP3A4 s=0.91 (rho=0.752). Rho-shrinkage -- provably optimal under squared error -- over-shrinks relative to ST-RAE's own optimum by a strikingly consistent ~0.11-0.17 s-units on every isoform, though the resulting ST-RAE cost is small in absolute terms (0.004-0.015 units), the same order of magnitude as notebook 21's own offset-only headroom. **Joint offset x spread grid (81x57, one per isoform)**: CYP1A2/CYP2D6/CYP3A4 show a small but material interaction between the two corrections per this notebook's stated 0.002-ST-RAE-unit bar (naive-vs-joint gaps 0.006/0.003/0.008); CYP2C9 is exactly separable (gap 0.000). **Part 3 (CYP2D6 only, notebook 21's OOF/blind population-mismatch caveat carried on every CYP2D6 number throughout)**: a second, independent sweep of the already-deployed corrected blind CYP2D6 column (verified mean=3.155762/std=0.557002 against `board_solved_calibration/submission_candidate.csv` directly) against rank-matched synthetic draws from the solved target Normal(3.1558, 1.5114) (5 seeds, logged, range checked for stability) finds an RAE-optimal further-expansion multiplier of s~2.77 (range 2.66-2.85) -- **the two CYP2D6 answers flatly disagree (OOF wants s=0.48, i.e. more shrinkage than raw; the solved-population exercise wants s~2.77, i.e. substantial further expansion of the already-shrunk column) and are reported side by side, not reconciled**, exactly as notebook 21's own population-mismatch finding would predict, and under this section's own stated idealised-rank-concordance approximation (likely optimistic, stated explicitly). COMPLETE -- report only, no correction applied to any blind prediction, no adoption recommended; `outputs/11_caruana_prep/`, `outputs/25_hard_compound_analysis/`, `outputs/21_strae_offset_curve/`, `outputs/calibration_blind_population/`, `outputs/population_moments/`, `outputs/board_solved_calibration/`, `outputs/19_cyp2c9_revert/`, `data/`, `src/` and every submission artifact confirmed untouched (`git status` after the run).
- **27**: **this is now the project's STANDING CALIBRATION NOTEBOOK.** Future placement/spread/board-metric work appends new numbered sections to this same notebook rather than spawning a new numbered notebook (its own header states this convention explicitly for a cold-start session). Existing notebooks `09`, `11b`, `13`, `14`, `16`, `21` and `26` are NOT merged into it and are never re-executed by it -- referenced read-only, superseded only for new work going forward. **Section 1 (2026-09-16)**: tests an external entrant's published claim (their own submissions, quoted as external data throughout, never this project's own) that their board ST-RAE is rank-ordered by how compressed each submitted column's SD is relative to training-label SD (their CYP2C9 1.15x ratio scores 0.5087; their CYP2D6 0.26x ratio scores 1.7940, worse than a constant predictor). Read-only, no training/Chemprop/torch, no submission built or sent. **A genuinely unspecified implementation choice was surfaced and flagged to the user rather than picked silently**: Part C's brief assumed a documented "submission format lower bound" for a clipping check, but none exists anywhere in this repo (`validate_activity_submission` checks only numeric/finite, not a pIC50 floor) -- user chose to report the minimum resulting value under each candidate multiplier rather than invent a threshold. **Part A**: all 7 real submissions' exact submitted CSVs (750 rows each, verified) loaded; board ST-RAE parsed programmatically from `docs/leaderboard_submissions.md` (not retyped -- 3 prior quoting errors in this project's history), with a macro-reconciliation self-check. External entrant's own training-SD figures matched this project's independently-computed training SDs to within 0.0005 on all four isoforms, supporting the two ratios are comparable (same released dataset). **Part B**: pooled Spearman(ratio, board_strae) across this project's own 28 submission-isoform points is real and robust -- rho=-0.822, 95% bootstrap CI [-0.888,-0.662] (custom percentile bootstrap, not scipy's `bootstrap()`, which returned an all-NaN CI on first attempt because a real fraction of resamples draw constant-x sets when a submission's column is byte-identical to another's -- an independence check found this is genuinely the case: e.g. CYP1A2/CYP3A4 have 10c=NB16=NB19 byte-identical, only 5 of 7 points per isoform distinct). Per isoform the naive p-values are misleading: CYP1A2 (p=0.0000, rho=-1.0) is a small-sample-with-ties artifact with a degenerate bootstrap CI of exactly [-1.0,-1.0]; CYP3A4 (p=0.0432) has a bootstrap CI, [-1.0, 0.125], wide enough to include near-zero; CYP2C9/CYP2D6 are not significant at all (p=0.465/0.188). **Only the pooled correlation is robust evidence; no single isoform's own submission history is, once ties and CI width are accounted for.** Confound stated explicitly and repeatedly: these 7 submissions differ in ensembling, environment migration, and 3 distinct calibration mechanisms, not spread alone -- any association is observational, consistent with but not proof of the compression hypothesis. This project's own rank-ordering matches the external entrant's claim only partially: the two sources agree at both extremes (CYP2D6 most-compressed-and-worst in both; highest-ratio isoform scores best in both), but the middle two isoforms invert (NB19: CYP1A2's ratio exceeds CYP2C9's, but CYP2C9 scores better). **Part C**: computed (not applied) mean-preserving spread multipliers for NB19 to reach ratio=1.0 and to the external entrant's best-scoring ratio (1.15, their CYP2C9); zero resulting predictions go negative on any isoform under either candidate, though CYP2D6/CYP3A4's minimums under the more aggressive multiplier (~0.67/0.42) sit well below any pIC50 ever observed in this project's own training data -- a plausibility flag, not a documented bound violation. Explicitly stated as a hypothesis about the blind optimum, not a measurement (notebook 21's OOF/blind mismatch caveat carried forward) -- no corrected file written, no submission built. **Part D figures**: `spread_ratio_vs_strae.png` (this project's 28 points colour-coded by isoform, external's 4 overlaid as open black X markers, ratio=1.0/ST-RAE=1.0 reference lines), `compression_by_isoform.png` (2x2, prediction SD vs. training SD per isoform across submissions in chronological order -- order verified against the doc's own submission-timestamp table, not notebook numbering: NB10 actually preceded 10c chronologically despite the numbering). **Part E verdict**: NB19 is compressed on all four isoforms (CYP3A4 least at 7% narrower, CYP2D6 most at 39% narrower); the pooled association holds robustly with the observational caveat attached; CYP1A2 (not CYP2D6) is named the more defensible candidate for a future widening test -- not because of a resolved internal statistic (none exists per isoform), but because it carries real, unaddressed compression with no prior deliberate calibration history, unlike CYP2D6 (already recentred, carries notebook 21's caveat) or CYP2C9 (this project's one clean monotonic-improvement isoform, per `docs/leaderboard_submissions.md`). No submission recommended, no correction applied, `src/calibration.py` untouched. `outputs/27_calibration/spread_ratio_table.csv`, `widening_multiplier_table.csv`, `figures/`. COMPLETE -- report only; `data/`, `src/`, every existing `outputs/` subdirectory, and every submission artifact confirmed untouched (`git status` after the run).

## Leaderboard submissions

Five real submissions to date: `04b` (macro ST-RAE 0.7179), `10c` (0.7138), notebook 10 (0.8299),
notebook 12 (0.8299), notebook 13's calibration submission — `NB13-calib` (0.8811).
**`docs/leaderboard_submissions.md` is the authoritative, full per-isoform,
per-metric (ST-RAE/MAE/R²/Spearman/Kendall) record of all five** — read it rather than
reconstructing scores from prose scattered across notebook markdown cells. Headline items from
that file: CYP2C9 is the only isoform where every metric improves or holds flat monotonically
across the first four submissions (04b→10c→NB10/NB12), a trend NB13-calib breaks; the
`cyp-admet-v2` environment migration is confirmed not to be the cause of notebook 10's regression
(10c vs. 04b); population calibration (every form tested, notebooks 09/11b/13/14) is now a
confirmed real-blind-data negative result, not just an OOF one; and all five submissions were
deliberate, reviewed, hand-run actions, not accidental (see the Rules-section
submission-cell-bug note).

## Key external references (beyond Zotero-tracked papers)
- Caruana, Niculescu-Mizil, Crew & Ksikes (2004), "Ensemble Selection from Libraries of Models," ICML'04 — core method for notebook 11.
- Dietterich (2000), "Ensemble Methods in Machine Learning" — accurate-and-diverse condition, residual-vs-prediction-correlation distinction.
- Lakshminarayanan, Pritzel & Blundell (2017), "Simple and Scalable Predictive Uncertainty Estimation using Deep Ensembles," NeurIPS — seed-ensembling reference. Note: this paper's own bagging (of training data) actively hurt their results — a different mechanism from Caruana's bagging (of the candidate library). Don't conflate the two when citing.